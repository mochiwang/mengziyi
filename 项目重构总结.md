# 项目重构总结 - ClassroomArchive 目录重组

## 重构目标
将纯课堂用途的文件统一移动到 `ClassroomArchive/` 目录下，保持公用工具/网络层/模型类型在原位，避免"牵一发动全身"。

## 移动的文件

### iOS客户端 (ziyi/)
**移动到 `ziyi/ClassroomArchive/`：**
- `ClassModeView.swift` - 课堂录音模式界面
- `RecorderManager.swift` - 录音管理器
- `StudyChatView.swift` - 学习助手界面

**保留在原位：**
- `APIClient.swift` - 通用网络层
- `NotesAPI.swift` - 笔记API封装
- `Models.swift` - 数据模型
- `ContentView.swift` - 根视图容器
- `ziyiApp.swift` - 应用入口点

### 后端服务 (menziyi-server/)
**移动到 `menziyi-server/ClassroomArchive/`：**
- `api/lessons.py` - 课程管理API
- `workers/transcribe_worker.py` - 转写工作进程

**保留在原位：**
- `api/notes.py` - 笔记处理API
- `api/chat.py` - 聊天检索API
- `core/` - 核心工具模块
- `app.py` - 主应用入口

## 更新的引用

### 后端引用更新
- `app.py` 中的导入路径：
  ```python
  # 更新前
  from api.lessons import bp as lessons_bp
  
  # 更新后
  from ClassroomArchive.lessons import bp as lessons_bp
  ```

### iOS项目文件更新
- 更新了 `ziyi.xcodeproj/project.pbxproj` 中的文件路径引用：
  - `ClassModeView.swift` → `ClassroomArchive/ClassModeView.swift`
  - `RecorderManager.swift` → `ClassroomArchive/RecorderManager.swift`
  - `StudyChatView.swift` → `ClassroomArchive/StudyChatView.swift`

## 验证结果

### 后端验证
```bash
cd menziyi-server
source venv/bin/activate
python3 -c "from ClassroomArchive.lessons import bp; print('Import successful')"
# 输出: Import successful

python3 -c "from ClassroomArchive.transcribe_worker import main; print('Transcribe worker import successful')"
# 输出: Transcribe worker import successful
```

### 项目结构
```
ziyi/
├── ClassroomArchive/          # 新建 - 课堂相关文件
│   ├── ClassModeView.swift
│   ├── RecorderManager.swift
│   └── StudyChatView.swift
├── APIClient.swift           # 保留 - 通用网络层
├── NotesAPI.swift            # 保留 - 笔记API
├── Models.swift              # 保留 - 数据模型
├── ContentView.swift         # 保留 - 根视图
└── ziyiApp.swift            # 保留 - 应用入口

menziyi-server/
├── ClassroomArchive/         # 新建 - 课堂相关文件
│   ├── lessons.py
│   └── transcribe_worker.py
├── api/                     # 保留 - 通用API
│   ├── notes.py
│   └── chat.py
├── core/                    # 保留 - 核心工具
├── app.py                   # 保留 - 主应用
└── workers/                 # 保留 - 其他工作进程
```

## 重构原则
1. **只移动纯课堂用途文件** - 避免影响其他功能模块
2. **保持公用工具在原位** - 确保其他模块的导入不受影响
3. **最小化影响范围** - 只更新必要的引用路径
4. **保持功能完整性** - 确保所有功能正常工作

## 后续建议
1. 在Xcode中重新打开项目，确保文件引用正确
2. 测试课堂录音和转写功能是否正常
3. 验证学习助手功能是否正常
4. 考虑在Xcode中创建ClassroomArchive组，更好地组织文件结构
