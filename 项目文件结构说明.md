# 孟子义项目文件结构说明

## 项目概述

孟子义是一个智能学习助手项目，采用前后端分离架构，包含iOS客户端、Flask后端服务器和一系列数据处理工具。本文档详细说明项目的文件结构和每个文件的功能职责。

## 整体目录结构

```
mengziyi/
├── Apple_Watch_MVP_开发文档.md     # Apple Watch MVP开发计划
├── iOS功能开关实现总结.md           # iOS功能开关技术总结
├── 项目重构总结.md                 # 项目重构过程总结
├── 项目总结文档.md                 # 项目整体总结文档
├── 项目文件结构说明.md             # 本文档
├── ziyi/                          # iOS客户端应用
├── menziyi-server/                # Flask后端服务器
├── notes/                         # 笔记处理流水线
├── prompts/                       # AI提示词模板
├── tools/                         # 数据处理工具
└── .venv/                         # Python虚拟环境
```

## 详细文件结构说明

### 1. 文档文件

#### 1.1 开发文档
- **`Apple_Watch_MVP_开发文档.md`**
  - **功能**: Apple Watch常驻采集MVP的详细开发计划
  - **内容**: 包含6个开发阶段、技术栈、架构流程图、风险评估等
  - **状态**: 开发进度跟踪文档

- **`iOS功能开关实现总结.md`**
  - **功能**: iOS应用功能开关的技术实现总结
  - **内容**: 功能开关的设计思路、实现方式、最佳实践
  - **用途**: 技术参考文档

- **`项目重构总结.md`**
  - **功能**: 项目重构过程的总结文档
  - **内容**: 重构原因、重构过程、重构结果
  - **用途**: 项目历史记录

- **`项目总结文档.md`**
  - **功能**: 项目整体功能和使用说明
  - **内容**: 项目概述、架构说明、核心组件详解、API接口文档
  - **用途**: 项目介绍和用户指南

### 2. iOS客户端 (ziyi/)

#### 2.1 应用核心文件
```
ziyi/
├── ziyiApp.swift                 # 应用入口点
├── ContentView.swift             # 主界面视图
├── Config.swift                  # 应用配置
├── Models.swift                  # 数据模型定义
├── APIClient.swift              # API网络客户端
├── NotesAPI.swift               # 笔记相关API
├── WCBridgePhone.swift          # WatchConnectivity iPhone端
└── Assets.xcassets/             # 应用资源文件
```

**详细说明:**
- **`ziyiApp.swift`**: 应用启动入口，配置应用生命周期
- **`ContentView.swift`**: 主界面，包含课程录制和学习模式入口
- **`Config.swift`**: 全局配置，如功能开关、API地址等
- **`Models.swift`**: 定义课程、笔记等数据模型
- **`APIClient.swift`**: 封装HTTP请求，处理与服务器的通信
- **`NotesAPI.swift`**: 专门处理笔记相关的API调用
- **`WCBridgePhone.swift`**: 实现WatchConnectivity，处理与Apple Watch的通信

#### 2.2 课程录制模块
```
ziyi/ClassroomArchive/
├── ClassModeView.swift           # 课程录制界面
├── StudyChatView.swift           # 学习聊天界面
└── RecorderManager.swift         # 录音管理器
```

**详细说明:**
- **`ClassModeView.swift`**: 课程录制的主界面，显示录制状态和控制按钮
- **`StudyChatView.swift`**: 课程学习界面，支持AI聊天和笔记查看
- **`RecorderManager.swift`**: 音频录制核心模块，管理录音会话和文件

#### 2.3 Apple Watch应用
```
ziyi/ziyiWatch Watch App/
├── ziyiWatchApp.swift            # Watch应用入口
├── ContentView.swift             # Watch主界面
├── WatchBridge.swift             # WatchConnectivity Watch端
└── Assets.xcassets/              # Watch应用资源
```

**详细说明:**
- **`ziyiWatchApp.swift`**: Apple Watch应用的启动入口
- **`ContentView.swift`**: Watch主界面，显示简单状态和控制
- **`WatchBridge.swift`**: Watch端的WatchConnectivity实现，处理与iPhone的通信

#### 2.4 项目配置文件
```
ziyi/ziyi.xcodeproj/              # Xcode项目文件
├── project.pbxproj               # 项目配置
├── project.xcworkspace/          # 工作空间配置
└── xcuserdata/                   # 用户数据
```

### 3. 后端服务器 (menziyi-server/)

#### 3.1 应用入口和配置
```
menziyi-server/
├── app.py                        # Flask应用主入口
├── requirements.txt              # Python依赖包
├── README.md                     # 服务器说明文档
└── venv/                         # Python虚拟环境
```

**详细说明:**
- **`app.py`**: Flask应用的主入口，配置应用、注册蓝图、设置中间件
- **`requirements.txt`**: 项目依赖包列表，包含Flask、Whisper等
- **`README.md`**: 服务器部署和使用说明

#### 3.2 API接口模块
```
menziyi-server/api/
├── lessons.py                    # 课程管理API
├── notes.py                      # 笔记处理API
└── chat.py                       # 聊天API
```

**详细说明:**
- **`lessons.py`**: 课程上传、状态查询、列表管理等API
- **`notes.py`**: 笔记分块、收集、标签、总结等处理API
- **`chat.py`**: AI聊天接口，支持关键词检索和智能回复

#### 3.3 核心功能模块
```
menziyi-server/core/
├── db.py                         # 数据库操作
├── transcriber.py                # 语音转写核心
├── notes_utils.py                # 笔记处理工具
├── paths.py                      # 路径管理
├── utils_media.py                # 媒体文件处理
└── utils_time.py                 # 时间处理工具
```

**详细说明:**
- **`db.py`**: SQLite数据库操作，课程状态管理
- **`transcriber.py`**: Whisper语音转写核心功能
- **`notes_utils.py`**: 笔记文件路径和内容处理工具
- **`paths.py`**: 统一管理项目中的文件路径
- **`utils_media.py`**: 音频文件处理、格式转换等
- **`utils_time.py`**: 时间格式化、时间戳处理等

#### 3.4 课程处理模块
```
menziyi-server/ClassroomArchive/
├── lessons.py                    # 课程处理蓝图
└── transcribe_worker.py          # 转写工作进程
```

**详细说明:**
- **`lessons.py`**: 课程相关的API蓝图，处理课程上传和状态
- **`transcribe_worker.py`**: 后台转写工作进程，异步处理音频转写

#### 3.5 数据存储
```
menziyi-server/data/
└── lessons/                      # 课程音频和转写结果
    ├── 2025-08-18/              # 按日期组织的课程数据
    ├── 2025-08-19/
    └── 2025-08-20/
```

**详细说明:**
- **`data/lessons/`**: 存储上传的音频文件和转写结果
- **按日期组织**: 每个日期一个文件夹，包含音频、转写文本、分段信息等

#### 3.6 数据库文件
```
menziyi-server/db/
└── index.sqlite                  # SQLite数据库文件
```

**详细说明:**
- **`index.sqlite`**: 存储课程信息、转写状态、元数据等

#### 3.7 测试文件
```
menziyi-server/
├── test_transcribe.py            # 转写功能测试
└── test_transcribe_no_vad.py     # 无VAD转写测试
```

**详细说明:**
- **`test_transcribe.py`**: 测试语音转写功能
- **`test_transcribe_no_vad.py`**: 测试不使用VAD的转写效果

### 4. 笔记处理流水线 (notes/)

#### 4.1 处理阶段目录
```
notes/
├── A_transcripts/                # 转写文本
│   └── lessons/                  # 课程转写文件
├── B_chunks/                     # 文本分块
├── C_labels/                     # 标签处理
├── Collected/                    # 收集的笔记
├── D_markdown/                   # Markdown输出
├── E_training/                   # 训练数据
├── Inbox/                        # 待处理文件
└── Processed/                    # 已处理文件
```

**详细说明:**
- **`A_transcripts/`**: 存储Whisper转写的原始文本
- **`B_chunks/`**: 文本分块处理结果
- **`C_labels/`**: 标签分类处理结果
- **`Collected/`**: 最终收集的结构化笔记
- **`D_markdown/`**: 生成的Markdown格式笔记
- **`E_training/`**: 用于AI训练的数据
- **`Inbox/`**: 新上传待处理的文件
- **`Processed/`**: 已完成处理的文件

### 5. AI提示词模板 (prompts/)

```
prompts/
├── chunk_map_prompt.txt          # 文本分块提示词
└── reduce_prompt.txt             # 文本摘要提示词
```

**详细说明:**
- **`chunk_map_prompt.txt`**: 用于文本分块和结构化的AI提示词
- **`reduce_prompt.txt`**: 用于文本摘要和压缩的AI提示词

### 6. 数据处理工具 (tools/)

```
tools/
├── build_dataset.py              # 构建训练数据集
├── make_md.py                    # 生成Markdown文件
└── split_chunks.py               # 文本分块工具
```

**详细说明:**
- **`build_dataset.py`**: 从处理后的笔记构建AI训练数据集
- **`make_md.py`**: 将结构化笔记转换为Markdown格式
- **`split_chunks.py`**: 将长文本分割成适合处理的小块

## 文件依赖关系

### 数据流向
```
iOS录制 → 上传到服务器 → Whisper转写 → 文本分块 → AI处理 → 结构化笔记 → Markdown输出
```

### 核心依赖
1. **iOS客户端** ↔ **Flask服务器**: 通过HTTP API通信
2. **iPhone** ↔ **Apple Watch**: 通过WatchConnectivity通信
3. **Flask服务器** → **Whisper**: 语音转写处理
4. **Flask服务器** → **Gemini API**: 文本处理和摘要
5. **笔记处理流水线**: 多阶段文本处理和数据转换

## 技术栈总结

### iOS端
- **Swift + SwiftUI**: 用户界面开发
- **AVFoundation**: 音频录制和播放
- **WatchConnectivity**: Apple Watch通信
- **URLSession**: 网络请求

### 服务端
- **Flask**: Web框架
- **faster-whisper**: 语音转写
- **SQLite**: 数据存储
- **ffmpeg**: 音频处理

### 数据处理
- **Python**: 脚本处理
- **JSON**: 数据交换格式
- **Markdown**: 文档输出格式

## 部署说明

### 开发环境
- iOS开发需要Xcode
- 服务端需要Python 3.8+
- 需要安装ffmpeg用于音频处理

### 生产环境
- 服务端部署在Mac mini
- 支持Docker容器化部署
- 需要配置SSL证书和反向代理

---

**文档版本**: v1.0  
**创建日期**: 2025-01-20  
**最后更新**: 2025-01-20  
**维护者**: 开发团队
